import { Injectable } from '@nestjs/common';
import { Cron } from '@nestjs/schedule';

@Injectable()
export class CheckersService {

   async checkProducts() {

   }
   // @Cron('0 * * * * *')
   // handleCron() {
   // }
}